import { Routes } from '@angular/router';
import { ReportListPage } from '@features/reports/presentation/pages/report-list/report-list.page';

export const REPORTS_ROUTES: Routes = [{ path: '', component: ReportListPage }];
